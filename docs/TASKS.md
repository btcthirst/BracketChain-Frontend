Головне що треба зробити:
1. Переписати cva варіанти в button.tsx під темну тему проєкту
2. Додати CSS змінні для кольорів у globals.css + tailwind.config
3. Створити <Modal> компонент (3 ідентичні overlay зараз)
4. Мігрувати файл за файлом (без breaking changes)

Висновок: Тема без зміни коду — пряме наслідування уніфікації. Якщо виконати Фазу 1 з docs/ui-unification.md (CSS токени → Tailwind utilities → компоненти споживають через класи), додавання нової теми стане одним @layer в globals.css без торкання жодного .tsx файлу. Поки компоненти хардкодять кольори — кожна нова тема вимагає 278+ правок.