import { AccountPage } from "@/features/account/AccountPage";

export const metadata = {
    title: "My Account — BracketChain",
    description: "Manage your linked accounts, wallet, and funds.",
};

export default function Page() {
    return <AccountPage />;
}
